spring:
  application:
    name: autodeploy-agent
agent:
  manifest-signing-key: ${MANIFEST_SIGNING_KEY:}
server:
  shutdown: graceful
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
